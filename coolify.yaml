apiVersion: coolify/v1
kind: Project
metadata:
  name: slynx-macro
  displayName: SLYNX Macro License Server
spec:
  runtime: docker
  services:
    - name: macro-auth
      type: web
      port: 8080
      build:
        # Coolify clones GitHub where this file is published as docker-compose.yml
        compose: docker-compose.yml
